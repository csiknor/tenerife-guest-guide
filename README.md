# Tenerife Guest Guide

Minimal Astro scaffold for a static, mobile-first apartment guest guide hosted on GitHub Pages.

## Development

```sh
npm install
npm run dev
```

Run `npm run build` to create the static site in `dist/`.

## Deployment

Push the `main` branch and select **GitHub Actions** as the Pages source in the repository settings. The included workflow builds and deploys the site.
